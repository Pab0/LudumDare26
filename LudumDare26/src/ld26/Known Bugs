1. calcFootprint() seems to spike every 3-4 circles at the footprint initialization of a different body each time (but only one).
2. Player can be smaller than all other bodies
3. java.lang.NegativeArraySizeException when swallowing two bodies simultaneously